// .vitepress/config.ts
import { defineConfig } from 'vitepress';

export default defineConfig({
  
  title: 'DILFYSCPS / CVMSCPS',
  description: 'Scenepack Directory for Dilfyscps & Cvmscps',
  // other configurations can go here
});